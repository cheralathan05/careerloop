// Minimal resume parser stub: expects text and extracts words matching skills
exports.parse = async (fileBuffer) => {
  // For now return a static list or parse text if provided
  return ['React', 'Node', 'JavaScript'];
};
